node_modules
dist
dist-ssr
*.local
.DS_Store

# Local dev TLS certs — never commit private keys
*.crt
*.key
*.pem

# Local dev channel store (server falls back to this when DATABASE_URL is unset)
server/channels.json

# Secrets — env only, never committed
.env
.env.*
