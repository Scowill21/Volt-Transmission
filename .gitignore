node_modules
dist
dist-ssr
*.local
.DS_Store

# Local dev TLS certs — never commit private keys
*.crt
*.key
*.pem

# Local dev channel + item stores (server falls back to these when DATABASE_URL is unset)
server/channels.json
server/items.json

# Secrets — env only, never committed
.env
.env.*

# shop purchases (test tier — 2b moves them to Postgres)
server/.shop-data.json
